export default function AuditPage() {
  return (
    <p className="rounded-md bg-[var(--surface)] p-4 text-[13px] text-[var(--muted)] ring-1 ring-[var(--border)] ring-inset">
      The audit trail, grouped by node, lands here in step 6.
    </p>
  );
}
